# Caminhos do Peregrino v1.0.1 — correção de arranque

Correção dirigida ao erro reportado no arranque:
`Cannot read properties of null (reading "lat")`.

Medidas:
- carregamento e validação do percurso e da base de apoios antes de iniciar o mapa;
- renderização do track e apoios com validação de dados;
- acesso à localização do utilizador protegido quando não existe posição;
- criação de limites Leaflet apenas com coordenadas válidas;
- mensagem de erro de arranque mais explícita em vez de exceção não tratada.

Validações realizadas:
- JavaScript `node --check`: OK.
- JSON de apoios: OK.
- GeoJSON do percurso: OK.
- import `PackageManager`: OK.
- 371 features de percurso presentes.
