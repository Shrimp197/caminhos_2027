The Centenario preparation hero image is supplied outside the text-only GitHub contents API during this V1 UI block. The runtime asset path will be wired once the binary blob is committed without replacing the official route geometry.